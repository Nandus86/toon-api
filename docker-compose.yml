version: '3.8'

services:
  toon-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: toon-converter-api
    working_dir: /app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    command: node index.js 
    restart: unless-stopped
    networks:
      - n8n

networks:
  n8n:
    external: true
